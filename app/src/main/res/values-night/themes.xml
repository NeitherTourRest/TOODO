<resources>
    <!-- Compose app: actual theming handled by ToodoTheme in Compose -->
    <style name="Theme.TOODO" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
